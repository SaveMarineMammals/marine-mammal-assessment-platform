{
  "name": "@mmap/brand",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared MMAP brand tokens and typography for web and field",
  "exports": {
    "./fonts.css": "./fonts.css",
    "./tokens.css": "./tokens.css"
  },
  "scripts": {
    "build": "node -e \"import('node:fs').then(fs=>{fs.mkdirSync('dist',{recursive:true});fs.writeFileSync('dist/.keep','');})\"",
    "dev": "node -e \"process.exit(0)\"",
    "test": "node -e \"process.exit(0)\"",
    "lint": "node -e \"process.exit(0)\""
  }
}
