# Optional: override API URL for cross-origin deployments.
# Leave unset for local dev and Docker — Vite/nginx proxy /v1 to the API.
# VITE_API_BASE_URL=http://localhost:3001

# Optional: external protocol guide URL (field app uses in-app /help/protocol by default).
# VITE_DOCS_URL=http://localhost:5173/docs/manatee-v1